const name = "Sehyeon",
  age = 31,
  gender = "male";

const sayHi = (name, age, gender) => {
  console.log(`Hello ${name}, your age is ${age}, and ${gender}`);
}

sayHi(name, age, gender);

export {};